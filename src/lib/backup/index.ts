/**
 * Backup Module
 * 
 * Exports backup and restore functionality for Sound Suite
 */

export { BackupManager } from './backup-manager';
export type { BackupManifest, BackupOptions, BackupResult } from './backup-manager';
